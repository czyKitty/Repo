 /***************************** commands ************************/
cat == 查看文件内容
	cat text | less  # 查看text文件中的内容  
cd == 切换目录
	cd /path    #切换到目录
	cd ./path   #切换到当前目录下
	cd ../path  #切换到上层目录
chown == 改变文件所有者
chmod == 改变文件权限
cp == 复制
	-a ：将文件的特性一起复制  
	-p ：连同文件的属性一起复制，而非使用默认方式，与-a相似，常用于备份  
	-i ：若目标文件已经存在时，在覆盖时会先询问操作的进行  
	-r ：递归持续复制，用于目录的复制行为  
	-u ：目标文件与源文件有差异时才会复制  
file == 判断文件数据
	file filename 
ls == 查看文件
      -l : 列出文件属性
      -a : 列出全部文件
      -d : 仅列出目录
      -h : 列出容量
man == 定义指令
       whatisthis
mkdir == 创建目录
	-m :  设置存取权限
	-p :  创建上层目录
mv == 移动文件
	-f ：直接覆盖  
	-i ：询问是否覆盖  
	-u ：更新  
rm == 删除文件/文件夹
	-i : 删除前确认
	-f : 直接删除
	-r : 清除目录下文件
su == 定义使用者
	whoami
tar == 压缩文件
	-c ：新建打包文件  
	-t ：查看打包文件内容
	-x ：解压缩
	-j ：通过bzip2的支持进行压缩/解压缩  
	-z ：通过gzip的支持进行压缩/解压缩  
	-v ：在压缩/解压缩过程中显示正在处理的文件名  
	-f filename ：filename为要处理的文件  
	-C dir ：指定压缩/解压缩的目录dir  
touch == 更新文件信息or创建空文件
	-a : 改变读取时间记录
	-m : 改变修改时间记录
	-c : 不建立新档案
	-r : 使用参考档的时间记录
	-d : 设定时间与日期
	-t : 设定时间
vim == 编辑文本


